%nav.navbar.navbar-fixed-top.navbar-default{role: "navigation"}
  .container-fluid
    .navbar-header
      %button.navbar-toggle{"data-target" => "#bs-example-navbar-collapse-1", "data-toggle" => "collapse", type: "button"}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand{href: root_path} Unturned Servers
    .test
      %ul.nav.navbar-nav
        %li.dropdown{class: set_active("servers")}
          %a.dropdown-toggle{"data-toggle" => "dropdown", href: "#"}
            %i.fa.fa-hdd-o
            Servers
            %i.fa.fa-caret-down
          %ul.dropdown-menu{role: "menu"} 
            %li= link_to 'All Servers', servers_path
            %li= link_to 'My Servers', servers_path(:owner => current_user)
            %li= link_to 'Search Servers', servers_path
        %li.dropdown{class: set_active("resources")}
          %a.dropdown-toggle{"data-toggle" => "dropdown", href: "#"}
            %i.fa.fa-book
            Resources
            %i.fa.fa-caret-down
          %ul.dropdown-menu{role: "menu"}
            %li= link_to 'Get Unturned', 'http://smartlydressedgames.com/unturned/'
            %li= link_to 'Unturned Wiki', 'http://unturned.wikia.com/wiki/Unturned_Bunker_Wiki'
            %li= link_to 'API Documentation', api_path
      %ul.nav.navbar-nav.navbar-right
        %li.dropdown
          %a.dropdown-toggle{"data-toggle" => "dropdown", href: "#"}
            - if current_user
              = current_user.username
            %i.fa.fa-user
          %ul.dropdown-menu{role: "menu"}
            - if current_user
              %li= link_to 'My Servers', servers_path
              %li= link_to 'View Profile', user_path(current_user.username)
              %li= link_to 'Edit Account', edit_user_registration_path(current_user)
              %li= link_to 'Logout', destroy_user_session_path, method: :delete
            - else
              %li= link_to 'Sign up', new_user_registration_path
              %li= link_to 'Login', new_user_session_path